---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: grants.postgresql-operator.vyrodovalexey.github.com
spec:
  group: postgresql-operator.vyrodovalexey.github.com
  names:
    kind: Grant
    listKind: GrantList
    plural: grants
    singular: grant
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Grant is the Schema for the grants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GrantSpec defines the desired state of Grant
            properties:
              database:
                description: Database is the PostgreSQL database name
                type: string
              defaultPrivileges:
                description: DefaultPrivileges is the list of default privileges for
                  future objects
                items:
                  description: DefaultPrivilegeItem defines default privileges for
                    future objects
                  properties:
                    objectType:
                      description: 'ObjectType is the type of object: tables, sequences,
                        functions, types'
                      type: string
                    privileges:
                      description: Privileges is the list of privileges to grant
                      items:
                        type: string
                      type: array
                    schema:
                      description: Schema is the schema name
                      type: string
                  required:
                  - objectType
                  - privileges
                  - schema
                  type: object
                type: array
              grants:
                description: Grants is the list of grants to apply
                items:
                  description: GrantItem defines a single grant
                  properties:
                    function:
                      description: Function is the function name (required for function
                        type)
                      type: string
                    privileges:
                      description: Privileges is the list of privileges to grant
                      items:
                        type: string
                      type: array
                    schema:
                      description: Schema is the schema name (required for schema,
                        table, sequence, function, all_tables, all_sequences types)
                      type: string
                    sequence:
                      description: Sequence is the sequence name (required for sequence
                        type)
                      type: string
                    table:
                      description: Table is the table name (required for table type)
                      type: string
                    type:
                      description: 'Type is the type of grant: database, schema, table,
                        sequence, function, all_tables, all_sequences'
                      enum:
                      - database
                      - schema
                      - table
                      - sequence
                      - function
                      - all_tables
                      - all_sequences
                      type: string
                  required:
                  - privileges
                  - type
                  type: object
                type: array
              postgresqlID:
                description: PostgresqlID is the ID of the PostgreSQL instance where
                  grants should be applied
                type: string
              role:
                description: Role is the PostgreSQL role/user to grant permissions
                  to
                type: string
            required:
            - database
            - postgresqlID
            - role
            type: object
          status:
            description: GrantStatus defines the observed state of Grant
            properties:
              applied:
                description: Applied indicates whether the grants have been successfully
                  applied in PostgreSQL
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                  of the Grant state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncAttempt:
                description: LastSyncAttempt is the timestamp of the last sync attempt
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
